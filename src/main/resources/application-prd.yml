# 生产环境配置
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.1.21.22:3306/2024_lzp?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: 2024_lzp
    password: bigdata22
    druid:
      initial-size: 10
      min-idle: 10
      max-active: 50
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

  redis:
    host: 10.1.21.22
    port: 6379
    password: bigdata22
    database: 1
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 16
        max-wait: -1ms
        max-idle: 16
        min-idle: 0

  cache:
    type: redis
    redis:
      time-to-live: 7200000
      cache-null-values: false

  thymeleaf:
    cache: true
